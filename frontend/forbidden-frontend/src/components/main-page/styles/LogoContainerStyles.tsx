import { styled } from "styled-components";

export const BigLogoStyles = styled.p`
	position: fixed;
	z-index: 100;
	display: flex;
	margin: 0;
	max-width: 100vw;
	color: white;
	white-space: nowrap;
`;
